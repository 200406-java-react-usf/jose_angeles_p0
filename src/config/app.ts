import {ArtistRepository} from '../repos/artist-repo';
import {ArtistService} from '../services/artist-service';

const artistRepo = new ArtistRepository();
const artistService = new ArtistRepository();


export default {
    artistRepo,
    artistService
};